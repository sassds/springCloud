spring:
  application:
    name: config_client
  cloud:
    config:
      # 配置中心服务的地址
      uri: http://localhost:10000
      # 要读取的配置文件application属性
      name: config_client
      # 要读取的配置文件profile属性,默认是dev
      profile: dev
#  rabbitmq:
#        host: localhost
#        port: 5672
#        username: user
#        password: secret
endpoints:
  refresh:
    enabled: true
