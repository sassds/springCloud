server:
  port: 10000
spring:
  application:
    name: config_server
  cloud:
    config:
      server:
        git:
#          uri: file://Volumes/Files/workspace/springCloud/config-repo
          uri: https://github.com/sassds/springCloud_config_repo

endpoints:
  refresh:
    enabled: true
